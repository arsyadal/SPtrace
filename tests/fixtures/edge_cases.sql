CREATE OR ALTER PROCEDURE dbo.SP_EDGE_CASES
    @ORDER_NO VARCHAR(20),
    @FLAG INT = 1 OUTPUT,
    @NAME NVARCHAR(50) = 'A, B, C'
AS
SELECT 1;

UPDATE TB_A SET NOTE = 'WHERE';
DELETE FROM TB_B WHERE NOTE = 'WHERE';
